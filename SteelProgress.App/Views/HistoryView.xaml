<UserControl x:Class="SteelProgress.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgMain}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- CABECERA -->
        <Border Grid.Row="0"
                Background="{StaticResource BgSecondary}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="22"
                Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Historial"
                           FontSize="30"
                           FontWeight="Bold"/>
                <TextBlock Text="Consulta tu último entrenamiento y compáralo con sesiones anteriores"
                           Foreground="{StaticResource TextSecondary}"
                           FontSize="15"
                           Margin="0,6,0,0"/>
            </StackPanel>
        </Border>

        <!-- SELECTOR DE SESIÓN -->
        <Border Grid.Row="1"
                Background="{StaticResource BgSecondary}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="20"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Sesión seleccionada"
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="0,0,0,8"/>

                    <ComboBox ItemsSource="{Binding Sessions}"
                              SelectedItem="{Binding SelectedSession, Mode=TwoWay}"
                              Height="40">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Date}" Margin="0,0,12,0"/>
                                    <TextBlock Text="{Binding RoutineDay.Name}"
                                               Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Ver progreso del ejercicio"
                        Width="220"
                        Height="40"
                        Margin="20,28,0,0"
                        Click="OpenSelectedExerciseProgress_Click"/>
            </Grid>
        </Border>

        <!-- RESUMEN DE ENTRENAMIENTO -->
        <Border Grid.Row="2"
                Background="{StaticResource BgSecondary}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="22">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Resumen del entrenamiento"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0,0,0,15"/>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding ExerciseSummaries}"
                         SelectedItem="{Binding SelectedExerciseSummary, Mode=TwoWay}"
                         Background="Transparent"
                         BorderThickness="0">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="Transparent"
                                    BorderBrush="{StaticResource BorderColor}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="16"
                                    Margin="0,0,0,12">

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel>
                                            <TextBlock Text="{Binding ExerciseName}"
                                                       FontSize="22"
                                                       FontWeight="Bold"/>

                                            <TextBlock Text="{Binding ComparisonText}"
                                                       Foreground="{StaticResource Accent}"
                                                       FontSize="14"
                                                       Margin="0,5,0,0"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1"
                                                    HorizontalAlignment="Right">
                                            <TextBlock Text="Mejor serie"
                                                       Foreground="{StaticResource TextSecondary}"
                                                       FontSize="12"
                                                       HorizontalAlignment="Right"/>
                                            <TextBlock Text="{Binding BestSetText}"
                                                       FontSize="20"
                                                       FontWeight="Bold"
                                                       HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Grid>

                                    <TextBlock Grid.Row="1"
                                               Text="Series realizadas"
                                               Foreground="{StaticResource TextSecondary}"
                                               FontSize="13"
                                               Margin="0,15,0,8"/>

                                    <ItemsControl Grid.Row="2"
                                                  ItemsSource="{Binding Sets}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>

                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{StaticResource BgMain}"
                                                        BorderBrush="{StaticResource BorderColor}"
                                                        BorderThickness="1"
                                                        CornerRadius="8"
                                                        Padding="10"
                                                        Margin="0,0,10,10">
                                                    <TextBlock FontSize="15"
                                                               FontWeight="SemiBold">
                                                        <Run Text="{Binding Weight}"/>
                                                        <Run Text=" kg x "/>
                                                        <Run Text="{Binding Reps}"/>
                                                    </TextBlock>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>