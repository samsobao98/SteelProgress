<UserControl x:Class="SteelProgress.App.Views.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             Background="{StaticResource BgMain}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource BgSecondary}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="22"
                Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Progreso del ejercicio"
                           FontSize="30"
                           FontWeight="Bold"/>

                <TextBlock Text="{Binding SelectedExercise.Name, TargetNullValue=Evolución del peso máximo por sesión}"
                           Foreground="{StaticResource TextSecondary}"
                           FontSize="16"
                           Margin="0,6,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Background="{StaticResource BgSecondary}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Peso máximo por sesión"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0,0,0,15"/>

                <lvc:CartesianChart Grid.Row="1"
                                    Series="{Binding Series}"
                                    XAxes="{Binding XAxes}"
                                    YAxes="{Binding YAxes}"
                                    Background="Transparent"
                                    LegendPosition="Hidden"
                                    AnimationsSpeed="0:0:0.5"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>