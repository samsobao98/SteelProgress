<Window x:Class="SteelProgress.App.RoutineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SteelProgress.App"
        mc:Ignorable="d"
        Title="Rutinas" Height="500" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Rutinas"
                   FontSize="24"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Nombre"/>
            <TextBox Text="{Binding Name, Mode=TwoWay}" Margin="0,5"/>
            
            <TextBlock Text="Notas"/>
            <TextBox Text="{Binding Notes, Mode=TwoWay}" Margin="0,5"/>
            
            <Button Content="Añadir rutina"
                    Command="{Binding AddRoutineCommand}" Margin="0,5"/>
            
            <Button Content="Eliminar rutina"
                    Command="{Binding DeleteRoutineCommand}" Margin="0,5"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Routines}"
                  SelectedItem="{Binding SelectedRoutine, Mode=TwoWay}"/>
    </Grid>
</Window>
